---
platform: linux

image_resource:
  type: docker-image

inputs:
- name: gpdb_src

outputs:
- name: gpAux_ext
- name: gphdfs_dist

run:
  path: gpdb_src/ci/concourse/scripts/get_ivy_dependencies.bash

params:
  TARGET_OS:
  TARGET_OS_VERSION:
  TASK_OS:
  TASK_OS_VERSION:
  IVYREPO_HOST:
  IVYREPO_REALM:
  IVYREPO_USER:
  IVYREPO_PASSWD:
