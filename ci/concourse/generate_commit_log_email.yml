---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/buildroot
    tag: git

inputs:
  - name: gpdb_src

outputs:
  - name: commit_log_email

run:
  path: gpdb_src/ci/concourse/scripts/generate_commit_log_email.sh
