platform: linux
inputs:
  - name: gpdb_src
  - name: bin_gpdb
  - name: sync_tools_gpdb
run:
  path: gpdb_src/ci/concourse/scripts/regression_tests_gphdfs.bash

params:
  HADOOP_TARGET_VERSION: cdh4.1