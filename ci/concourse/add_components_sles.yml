---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: pivotaldata/sles-gpdb-dev
    tag: 11-beta

inputs:
  - name: gpdb_src
  - name: bin_gpdb
  - name: component_gpbackup
  - name: component_gpcopy_helper

outputs:
  - name: bin_gpdb_with_components

run:
  path: gpdb_src/ci/concourse/scripts/add_components.sh

