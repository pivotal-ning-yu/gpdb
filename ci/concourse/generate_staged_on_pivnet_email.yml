---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/buildroot
    tag: git

inputs:
  - name: gpdb_src

outputs:
  - name: staged_on_pivnet_email

run:
  path: gpdb_src/ci/concourse/scripts/generate_staged_on_pivnet_email.sh
