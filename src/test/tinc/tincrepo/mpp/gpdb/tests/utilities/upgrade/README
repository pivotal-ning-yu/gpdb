These are the 4.3.x upgrade tests.  Since 4.2 is no longer supported,
we mainly only test for binary swap compatibility.

Set environment variable GPDB_COPY_BIN_FROM to the location of the old
Greenplum 4.3.x binary.  The tests will automatically acquire your
latest Greenplum 4.3.x via $GPHOME.
